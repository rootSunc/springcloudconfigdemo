spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ericdahl/spring-cloud-config-example
          search-paths: server-config